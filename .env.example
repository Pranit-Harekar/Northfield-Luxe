# Google AdSense (optional — the shop page's hero ad carousel only renders
# when both values are set). Copy this file to .env.local (git-ignored) and
# fill in your real values; never commit real AdSense ids.
NEXT_PUBLIC_ADSENSE_CLIENT_ID=
NEXT_PUBLIC_ADSENSE_HERO_SLOT_ID=
